# ============================================================
# REDFORGE-02 CONFIGURATION CONTEXT
# ============================================================

# Hostname
hostname

# IP configuration
ip a

# Default route
ip route

# Confirm attack interface
ip a | grep inet

# Verify Nmap version
nmap --version

# Verify user context (important for scan type)
whoami
id
